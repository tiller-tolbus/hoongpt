{
    "question": "Write a gate that takes a string (`tape`) and returns a loobean (`%.y` or `%.n`) indicating whether the string is a palindrome (reads the same forwards and backwards). Assume the string contains only lowercase letters and no spaces or punctuation.",
    "tests": [
        {
            "input": "racecar",
            "expected": "%.y"
        },
        {
            "input": "urbit",
            "expected": "%.n"
        },
        {
            "input": "deified",
            "expected": "%.y"
        },
        {
            "input": "noon",
            "expected": "%.y"
        }
    ]
}